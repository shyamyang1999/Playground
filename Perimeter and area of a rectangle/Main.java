#include<stdio.h>
int main()
{
  int l=6,b=9,per,area;
  per=2*(l+b);
  area=l*b;
  printf("The perimeter of the rectangle is: %d cm\n",per);
  printf("The area of the rectangle is: %d sq cm",area);
  return 0;
}